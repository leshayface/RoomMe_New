<%= form_for(housemate) do |f| %>
  <% if housemate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(housemate.errors.count, "error") %> prohibited this housemate from being saved:</h2>

      <ul>
      <% housemate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
