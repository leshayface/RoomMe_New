<%= form_for(about_property) do |f| %>
  <% if about_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(about_property.errors.count, "error") %> prohibited this about_property from being saved:</h2>

      <ul>
      <% about_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
